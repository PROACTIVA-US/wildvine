---
summary: "CLI reference for `wildvine vinebot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `wildvine vinebot ...`
  - You need migration guidance to current commands
title: "vinebot"
---

# `wildvine vinebot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `wildvine vinebot qr` (same behavior as [`wildvine qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `wildvine vinebot qr` -> `wildvine qr`
